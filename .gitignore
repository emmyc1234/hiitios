# Node / Vite
node_modules/
dist/
!.keep

# macOS
.DS_Store

# Xcode
DerivedData/
xcuserdata/

# CocoaPods (let CI run `pod install`; do commit Podfile + Podfile.lock)
Pods/

# Capacitor build outputs (native build products)
ios/App/build/

# Replit / editor clutter
.replit
.vscode/
.idea/
