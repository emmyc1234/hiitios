import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hiitworkout.app',
  appName: 'HIIT Workout',
  webDir: 'dist/public'
};

export default config;
